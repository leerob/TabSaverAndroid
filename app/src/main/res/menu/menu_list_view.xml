<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:search="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/search"
        android:title="@string/search"
        android:icon="@drawable/ic_search"
        search:showAsAction="collapseActionView|ifRoom"
        search:actionViewClass="com.tabsaver.Adapters.ArrayAdapterSearchView" />

    <item
        android:id="@+id/showMapView"
        android:icon="@drawable/ic_map_white_24dp"
        android:title="Map"
        search:showAsAction="ifRoom" />

    <item android:id="@+id/action_settings"
        android:icon="@drawable/ic_settings_black_24dp"
        search:showAsAction="ifRoom"
        android:title="@string/action_settings" />

</menu>
